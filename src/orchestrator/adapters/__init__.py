"""Adapters layer for external system integration.

This package contains adapters that bridge the orchestrator domain
with external systems like CLI arguments, configuration files,
and Docker API.
"""
