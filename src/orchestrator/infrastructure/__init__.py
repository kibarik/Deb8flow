"""Infrastructure layer for external services.

This package contains implementations that interact with external
services like Docker containers, Claude Code CLI subprocesses,
and filesystem operations.
"""
